info:
  name: Get Country by Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/countries/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
