info:
  name: Get All Countries
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/countries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
