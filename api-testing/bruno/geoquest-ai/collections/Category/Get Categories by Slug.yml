info:
  name: Get Categories by Slug
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/categories/slug/countries-and-continents"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const body = res.body;

        test("Status code is 200", () => {
          expect(res.getStatus()).to.equal(200);
        });

        test("Category contains expected fields", () => {
          expect(body).to.have.property("id");
          expect(body).to.have.property("name");
          expect(body).to.have.property("categorySlug");
          expect(body).to.have.property("createdAt");
          expect(body).to.have.property("updatedAt");
        });

        test("Category name is not empty", () => {
          expect(body.name.length).to.be.greaterThan(0);
        });

        test("Category blob is not empty", () => {
          expect(body.categorySlug.length).to.be.greaterThan(0);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
