info:
  name: Soft Delete Question
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/questions/jvhgi46wpvpy0apm9m1y7w3n"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", () => {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
